// This file is no longer used and can be deleted.
// The root path '/' now redirects to '/feed' via middleware.
// The AgeGate component is now managed within the root layout.
export default function HomePage() {
  return null;
}
