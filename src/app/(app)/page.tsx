// This file is intentionally left blank.
// The root of the (app) group does not need a page.
// The middleware will handle redirection to a default page like /dashboard.
export default function AppRootPage() {
  return null;
}
